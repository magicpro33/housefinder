# Copy to secrets.toml for local testing (DO NOT commit secrets.toml).
# On Streamlit Cloud, paste this into: App settings → Secrets

RENTCAST_API_KEY = "your_rentcast_key_here"
RENTCAST_MONTHLY_LIMIT = "50"
